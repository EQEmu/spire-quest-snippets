for k, v in ipairs(${1:table_name}) do -- For each key-value pair in table, do something
    ${2:logic} -- Example of using key and value: eq.message(Chat.White, string.format("Key: %s Value: %s", tostring(k), tostring(v)))
end